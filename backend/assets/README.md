# Certificate Logos

These files ship with the backend when Railway deploys only the backend directory.
Set NEXT_PUBLIC_LOGO_LIGHT to the exact filename to use for certificates.
There is no fallback logo. Add replacement certificate logos here, even if the
frontend also has a copy in public/assets.
