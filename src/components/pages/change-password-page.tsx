"use client"; import { FormEvent,useState } from "react"; import { api,jsonBody } from "@/lib/api"; import { useAuth } from "@/components/auth-provider"; import { ErrorMessage,PageHeader } from "@/components/ui";
export function ChangePasswordPage(){const {refresh}=useAuth();const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const values=Object.fromEntries(new FormData(e.currentTarget));if(values.new_password!==values.confirm_password){setError("Passwords do not match.");return}try{await api("/auth/change-password/",{method:"POST",body:jsonBody(values)});await refresh();window.location.assign("/dashboard")}catch(x){setError(x instanceof Error?x.message:"Unable to change password")}}return <div className="space-y-6"><PageHeader title="Change Password" description="Set a private password before continuing."/><form className="panel p-6 max-w-lg grid gap-4" onSubmit={submit}><input className="field" name="current_password" type="password" placeholder="Temporary password" required/><input className="field" name="new_password" type="password" placeholder="New password" minLength={8} required/><input className="field" name="confirm_password" type="password" placeholder="Confirm new password" required/>{error&&<ErrorMessage message={error}/>}<button className="btn btn-primary">Change password</button></form></div>}
