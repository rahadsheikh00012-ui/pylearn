"use client";
import { api,jsonBody,unwrap } from "@/lib/api"; import { useApiData } from "@/hooks/use-api-data"; import { Empty,ErrorMessage,Loading,PageHeader } from "@/components/ui";
type A={reference:string;full_name:string;email:string;phone:string;bachelor_degree:string;master_degree:string;years_experience?:number;expertise:string;teaching_background:string;status:string;admin_note:string;created_at:string};
export function InstructorApplicationsPage(){const list=useApiData<A[]|{results:A[]}>("/instructor-applications/");async function review(form:HTMLFormElement,a:A,decision:"approve"|"reject"){const values=Object.fromEntries(new FormData(form));await api(`/instructor-applications/${a.reference}/${decision}/`,{method:"POST",body:jsonBody(values)});await list.reload()}
return <div className="space-y-6"><PageHeader title="Instructor Applications" description="Review, approve, or reject teacher applications."/>{list.loading?<Loading/>:list.error?<ErrorMessage message={list.error}/>:!unwrap(list.data||[]).length?<Empty message="No applications."/>:<div className="space-y-4">{unwrap(list.data||[]).map(a=><article className="panel p-5" key={a.reference}><div className="flex justify-between gap-4"><div><h2 className="font-bold">{a.full_name}</h2><p className="muted text-sm">{a.email} · {a.phone} · {a.reference}</p></div><span className="badge">{a.status}</span></div><p className="mt-3"><strong>Bachelor:</strong> {a.bachelor_degree}{a.master_degree&&` · Master: ${a.master_degree}`}</p><p><strong>Experience:</strong> {a.teaching_background}</p>{a.expertise&&<p><strong>Skills:</strong> {a.expertise}</p>}{a.status==="PENDING"&&<form className="mt-4 grid gap-2 sm:grid-cols-3" onSubmit={e=>{e.preventDefault();void review(e.currentTarget,a,"approve")}}><input className="field" name="password" type="password" placeholder="Temporary password" required/><input className="field" name="admin_note" placeholder="Admin note"/><button className="btn btn-primary">Approve</button><button type="button" className="btn btn-danger sm:col-start-3" onClick={e=>{const form=e.currentTarget.form;if(form)void review(form,a,"reject")}}>Reject</button></form>}</article>)}</div>}</div>}
